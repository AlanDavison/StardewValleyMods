name: Build Invisible Buffs

on:
    workflow_dispatch:

run-name: Build Invisible Buffs.
permissions:
    contents: write
    id-token: write
    attestations: write

jobs:
    build:
        uses: ./.github/workflows/build-mod.yml
        with:
            project_name: "InvisibleBuffs"
            create_draft_release: true
